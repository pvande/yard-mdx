<% info = indent(yieldall) -%>
<% if info.empty? -%>
<Card icon=<%= icon_for(object).inspect %> title=<%= object.title.inspect%> />
<% else -%>
<Card icon=<%= icon_for(object).inspect %> title=<%= object.title.inspect%>>
<%= info %>
</Card>
<% end %>
