<% tags = object.tags(:option).select { |x| x.name.to_s == options.index } -%>
<% param, default = object.parameters.find { |param, default| options.index == param.to_s.sub(/^\*+|:$/, '') } -%>
<% tags.each do |tag| -%>
* <%= indent(param_component(tag.pair)).lstrip %>
<% end -%>
